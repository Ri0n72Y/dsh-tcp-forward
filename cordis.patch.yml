- insert:
    - id: tcp-forward
      name: dsh-tcp-forward

- id: connection
  inject: [webRuntime, tcpForward]
  config:
    trustedHosts: !!js [...ctx.webRuntime.trustedHosts, ...ctx.tcpForward.trustedHosts]
